export interface IPerson {
    firstName: string,
    middleName: string,
    lastName: string
}//typically, but not necessarily, an interface will start with a capital I to indicate that it's an interface